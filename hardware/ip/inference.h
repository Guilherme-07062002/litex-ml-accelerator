// Declarações da função de inferência
#ifndef INFERENCE_H
#define INFERENCE_H

#include <stdint.h>

// Função principal de inferência
// Retorna o valor predito pela rede neural para a entrada x
float predict(float x);

#endif // INFERENCE_H
